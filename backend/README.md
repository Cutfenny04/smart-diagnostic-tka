# Backend — Smart Diagnostic TKA

Dokumentasi lengkap (arsitektur, database, autentikasi, referensi endpoint, environment variables, cara menjalankan) ada di **[README.md di root repo](../README.md)** — dokumen ini sengaja tidak diduplikasi di sini supaya tidak ada dua sumber yang bisa berbeda satu sama lain.

Ringkas untuk mulai cepat:

```bash
npm install
cp .env.example .env    # isi sesuai README root § Environment Variables
npm run dev
```
